# Haven Pages Redirect

Haven's canonical app surface is the Worker at:

https://haven.lbourgon.workers.dev

This directory is only for deploying the legacy Cloudflare Pages project
`haven` (`haven-du5.pages.dev`) as a permanent redirect to the Worker-hosted
app. Do not deploy the main React dashboard to Pages unless Haven is
intentionally split back into separate frontend and API origins.
